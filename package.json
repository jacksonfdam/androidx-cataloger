{
  "name": "androidx-scraper",
  "version": "1.0.0",
  "description": "Web service to scrape and catalog AndroidX libraries",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src --ext js,njk,json src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "body-parser": "^1.20.2",
    "cheerio": "^1.0.0-rc.12",
    "express": "^4.18.2",
    "mongoose": "^8.0.1",
    "nunjucks": "^3.2.4",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
